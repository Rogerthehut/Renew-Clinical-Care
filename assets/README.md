# Image assets needed here

`index.html` references these filenames — add the real image files with
exactly these names and they'll show up immediately, no HTML changes needed:

- `rcc-logo.png` — header/footer logo
- `care-at-home.png` — poster image shown before the first hero video loads
- `postnatal-support-home.webp` — hero montage frame
- `paediatric-nurse-home.webp` — hero montage frame + second video's poster
- `rcc-mark.png` (optional) — faint watermark referenced in styles.css's `.panel:after`

Delete this README once the real assets are in place.
